<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.duoduocode.service.common.mapper.UserDataHideMapper">

    <insert id="insert" parameterType="com.duoduocode.service.common.entity.UserDataHide" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_data_hide (user_id, data_type, ref_id, created_at)
        VALUES (#{userId}, #{dataType}, #{refId}, NOW())
    </insert>

    <select id="selectByUserAndRef" resultType="com.duoduocode.service.common.entity.UserDataHide">
        SELECT id, user_id AS userId, data_type AS dataType, ref_id AS refId, created_at AS createdAt
        FROM user_data_hide
        WHERE user_id = #{userId} AND data_type = #{dataType} AND ref_id = #{refId}
    </select>

</mapper>
